
En este ejercicio vamos a crear con tres containers corriendo un servicio web y un servicio que contabilice
 las visitas ejecutando sobre redis.

Para ello vamos a ir agregando al docker-compose.yml varios parámetros necesarios ...

En el fichero docker-compose.yml de este directorio tenemos dos errores. Resuelve y haz que funcione el cluster.